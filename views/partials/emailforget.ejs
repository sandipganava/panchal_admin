<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />

    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Static Template</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            margin: 0;
            font-family: 'Poppins', sans-serif;
            background: #ffffff;
            font-size: 14px;
        }

        .container {
            max-width: 680px;
            margin: 0 auto;
            padding: 45px 30px 60px;
            background: #f4f7ff;
            background-image: url(https://archisketch-resources.s3.ap-northeast-2.amazonaws.com/vrstyler/1661497957196_595865/email-template-background-banner);
            background-repeat: no-repeat;
            background-size: 800px 452px;
            background-position: top center;
            font-size: 14px;
            color: #434343;
        }

        header table {
            width: 100%;
        }

        header tr {
            height: 0;
        }

        header td:last-child {
            text-align: right;
        }

        header span {
            font-size: 16px;
            line-height: 30px;
            color: #ffffff;
        }

        .main {
            margin: 0;
            margin-top: 70px;
            padding: 92px 30px 115px;
            background: #ffffff;
            border-radius: 30px;
            text-align: center;
        }

        .main div {
            width: 100%;
            max-width: 489px;
            margin: 0 auto;
        }

        h1 {
            margin: 0;
            font-size: 24px;
            font-weight: 500;
            color: #1f1f1f;
        }

        p {
            margin: 0;
            margin-top: 17px;
            font-size: 16px;
            font-weight: 500;
        }

        p span {
            font-weight: 600;
            color: #1f1f1f;
        }

        #otpCopy {
            font-size: 40px;
            font-weight: 600;
            letter-spacing: 11px;
            color: #ba3d4f;
            margin-left: 45px;
        }
    </style>
</head>

<body>
    <div class="container">
        <header>
            <table>
                <tbody>
                    <tr>
                        <td></td>
                        <td>
                            <span><%= date%></span>
                        </td>
                    </tr>
                </tbody>
            </table>
        </header>

        <main>
            <div class="main">
                <div>
                    <h1>Your OTP</h1>
                    <p>Hey <%= name %>,</p>
                    <p>Thank you for choosing CodecrewInfoTech Company. Use the following OTP to complete the procedure to change your email address. OTP is valid for <span>10 minutes</span>. Do not share this code with others, including CodecrewInfoTech employees.</p>
                    <div style="display: flex; align-items: center;  margin-top: 60px; justify-content: center;">
                        <p id="otpCopy"><%= otp%></p>
                    </div>
                </div>
            </div>
        </main>
    </div>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#copyButton').click(function () {
                var textToCopy = $('#otpCopy').text();
                var tempInput = $('<input>');
                $('body').append(tempInput);
                tempInput.val(textToCopy).select();
                document.execCommand('copy');
                tempInput.remove();
            });
        });
    </script>
</body>

</html>
